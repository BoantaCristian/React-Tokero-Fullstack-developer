{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DevConnection": "Server=(local)\\sqlexpress;Database=BallanceDB;Trusted_Connection=True;MultipleActiveResultSets=True;"
  }                   //declare the connection to server as a local conection: sql express needed to be installed and run in background processes of machine
}                     //then the database name is specified, the connection will be marked as a trusted and the server will allow execution of multiple batches on a single connection
